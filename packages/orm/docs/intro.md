## Introduction

`@gritjs/orm` is a lightweight, type-safe ORM for Node.js. It provides a
unified API for working with relational and document databases through
pluggable connectors.

## Installation

```bash
npm install @gritjs/orm
```
